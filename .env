DATABASE_NAME=work
DATABASE_URL=./work.db
DATABASE_DRIVER=sqlite3
TURSO_TOKEN=add-a-turso-token
PROD_DATABASE_URL=add-a-prod-database-url
PROD_DATABASE_DRIVER=libsql

# Git analysis prompt for directory processing
GIT_ANALYSIS_PROMPT=Review git commits between {from_date} and {to_date}. Provide a concise summary of what was accomplished - focus on the actual work done, not implementation details. Use simple, business-friendly language. Maximum 2-3 short bullet points. If no commits, respond as follows: NO COMMITS.

# Dev mode - creates persistent temp directories for inspection
DEV_MODE=true
